.show-title
  = @article.title
.show-content
  = simple_format(@article.text)
.show-image
  - if @article.images.present?
    .article_image
      - @article.images.each do |image|
        = image_tag image.image.url
.link_edit
  = link_to '記事を編集する', edit_article_path(@article)
  = link_to "削除する", article_path(@article), method: :delete,class:"destroy-btn"
